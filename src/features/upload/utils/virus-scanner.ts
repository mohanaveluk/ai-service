// Virus scanning utility
export class VirusScanner {
  static async scan(buffer: Buffer): Promise<boolean> {
    // Simulate virus scan (replace with actual ClamAV or external service)
    return true;
  }
}