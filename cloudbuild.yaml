steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/ai-api-service', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ai-api-service']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'ai-api-service'
      - '--image'
      - 'gcr.io/$PROJECT_ID/ai-api-service'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--cpu=2'
      - '--memory=2Gi'
      - '--set-env-vars'
      - '--min-instances=0'
      - '--max-instances=5'
      - 'DB_HOST=${_DB_HOST},DB_PORT=${_DB_PORT},DB_USERNAME=${_DB_USERNAME},DB_PASSWORD=${_DB_PASSWORD},DB_DATABASE=${_DB_DATABASE},NODE_ENV=${_NODE_ENV},DB_MIGRATIONS_RUN=${_DB_MIGRATIONS_RUN}'

images:
  - 'gcr.io/$PROJECT_ID/ai-api-service'
    
options:
  logging: CLOUD_LOGGING_ONLY